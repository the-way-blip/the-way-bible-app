# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build scripts — ignored by default, except the atlas generator and the
# dataset it compiles from (src/data/biblePlaces.json is committed, so the
# script that produces it should be too).
scripts/*
!scripts/build-bible-places.mjs
!scripts/data/
scripts/data/*
!scripts/data/openbible-merged.txt
.vercel
